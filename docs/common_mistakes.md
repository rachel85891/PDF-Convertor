# Common Mistakes

- Mixing Type A and Type B layout assumptions in one renderer path.
- Parsing by fixed column index without header-based mapping.
- Ignoring RTL alignment when generating HTML/PDF.
- Falling back silently on generation errors without clear logging.
- Storing business rules inside templates instead of transformation layer.
